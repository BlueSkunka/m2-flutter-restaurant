meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{host}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "entrepreneur@entrepreneur.com",
    "password": "test"
  }
}

script:pre-request {
  const data = res.getBody();
  bru.setEnvVar("token",data.accessToken)
}
