meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{host}}/reservations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "test",
    "capacity": 3
  }
}
