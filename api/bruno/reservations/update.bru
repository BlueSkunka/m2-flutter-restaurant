meta {
  name: update
  type: http
  seq: 4
}

put {
  url: {{host}}/reservations/:id
  body: json
  auth: bearer
}

params:path {
  id: 2
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "test mathis",
    "capacity": 10
  }
}
